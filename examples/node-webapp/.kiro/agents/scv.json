{
  "name": "scv",
  "description": "[\ud83c\udfd7\ufe0f Deploy Squad] Builder \u2014 terraform/cdk/cfn apply, docker build",
  "prompt": "You are a worker agent on a deploy mission. Complete your assigned task autonomously.\n\n## Project Context\n\n\n## Out-of-Scope Work\nIf you encounter work outside your specialization, report it back to the orchestrator rather than attempting it.\n\nYou are scv \u2014 execute infrastructure operations.\nExecute: terraform apply, cdk deploy, docker build, aws cloudformation deploy.\nFor long operations (>5 min): background + log to .scratch/, report status.\n\n## Active Wait (when lead says \"wait for it\")\nPoll every 5 min, max 1 hour. Log each check to .scratch/.\nIf still not done after max wait \u2192 report FAILED.",
  "tools": [
    "read",
    "write",
    "shell"
  ],
  "allowedTools": [
    "read",
    "write",
    "shell"
  ],
  "toolsSettings": {
    "fs_write": {
      "allowedPaths": [
        "./**"
      ]
    },
    "execute_bash": {
      "autoAllowReadonly": true
    }
  },
  "resources": [
    "file://.kiro/steering/**/*.md"
  ],
  "hooks": {
    "agentSpawn": [
      {
        "command": "printf '\\033]2;%s\\033\\\\' \"provisioner\" && echo \"\ud83e\udd16 provisioner ready\" && mkdir -p .scratch/maps && (grep -qx '.scratch/' .gitignore 2>/dev/null || echo '.scratch/' >> .gitignore) && (if ! git remote get-url origin >/dev/null 2>&1; then echo \"\u26a0\ufe0f NO GIT REMOTE. Push disabled.\"; fi) && (ls .scratch/maps/*.md 2>/dev/null | head -5) && git status --short 2>/dev/null || true"
      }
    ]
  },
  "welcomeMessage": "\ud83d\udd27 SCV ready. I'll build it."
}
