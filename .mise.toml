min_version = "2024.1.0"

[tools]
python = "3.12"
uv = "latest"

[tasks.lint]
run = "ruff check *.py scripts/*.py"
description = "Lint Python files"

[tasks."lint:fix"]
run = "ruff check --fix *.py scripts/*.py"
description = "Auto-fix lint issues"
